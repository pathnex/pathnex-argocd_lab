apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: pathnex-grafana
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/pathnex/ArgoCd_lab.git'
    targetRevision: HEAD
    path: grafana
    helm:
      valueFiles:
        - values.yaml
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: monitoring
  syncPolicy:
    automated:
      prune: true
      selfHeal: true